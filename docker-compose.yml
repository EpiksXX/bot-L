version: '3'
services:
  jarvis-bot:
    build: .
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - PHONE_IP=${PHONE_IP}
    volumes:
      # Сохраняем папку /app/data из контейнера прямо на жесткий диск мини-ПК
      - bot_memory:/app/data

volumes:
  bot_memory:
